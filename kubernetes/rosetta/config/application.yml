server:
  port: 4923

logging:
  pattern:
    level: "%5p %X{CorrelationId}"
  level:
    root: warn
    org.elasticsearch.client: error
    com.k_int.rosetta: info
    com.k_int.rosetta.web.api.iiif: debug
    com.k_int.rosetta.web.iiif.presentation.v3: debug
    com.k_int.rosetta.provider.elastic: debug

management:
  endpoints:
    web:
      exposure:
        include:
          - health

springdoc:
  api-docs:
    path: /api/v1/spec

rosetta:
  cache:
    enabled: false
    use_cache_service: false
  transform:
    enabled: true
    timeout_millis: 6000
    glyphs:
      - name: jpt
        type: jpt
        properties:
          mutable: true
          file: ./config/jpt.json
          input: source
          output: template
  provider:
    providers:
      - name: elasticsearch
        type: elasticsearch
        domainConverter: es-converter
        properties:
          host: ciim-internal-es-http.etna
          port: 9200
          index: ciim
          username: elastic
          password: 2575T63o2BARFNOUlp5T28Ih
          path_config:
            paths:
              topic: "topic.summary.title.keyword"
              collection: "@hierarch.identifier.reference_number"
              group: "@datatype.group.value"
              level: "level.value"
              type: "@datatype.actual"
              country: "place.@hierarchy.name.value.keyword"
              closure: "availability.closure.label.value.keyword"
              catalogueSource: "source.value"
              heldBy: "repository.name.value.keyword"


  domain:
    converters:
      - name: es-converter
        type: elastic-default
