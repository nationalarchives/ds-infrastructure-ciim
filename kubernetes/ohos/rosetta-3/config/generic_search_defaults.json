{
  "#type": "merge",
  "values": [
    "$.request",
    {
      "aggregations": {
        "#type": "as_list",
        "values": {
          "#type": "require",
          "require": "$.request[?(!(@.ids))]",
          "value": [
            "group"
          ]
        }
      }
    }
  ]
}