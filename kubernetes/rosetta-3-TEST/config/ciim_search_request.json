{
  "results": {
    "request": {
      "query": "$.query",
      "aggregations": "$.aggregations",
      "from": "$.from",
      "size": "$.size",
      "sort": "$.sort"
    },
    "cache": {
      "name": "fast-cache"
    }
  },
  "aggs": {
    "request": {
      "query": {
        "bool": {
          "must": "$.query.bool.must"
        }
      },
      "aggregations": {
        "group": {
          "terms": {
            "field": "@datatype.group.value",
            "size": 50
          }
        },
        "longSubjects": {
          "terms": {
            "field": "subjects.subject.summary.title",
            "size": 100
          }
        },
        "longArchives": {
          "terms": {
            "field": "repository.name.value.keyword",
            "size": 100
          }
        },
        "longCollections": {
          "terms": {
            "field": "@hierarch.identifier.value",
            "size": 100
          }
        }
      },
      "size": 0,
      "timeout": "60s"
    },
    "cache": {
      "name": "slow-cache"
    }
  }
}
